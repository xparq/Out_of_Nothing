
The build system must be able to selectively include only one of these dirs,
depending on the configured backend!

Just renaming/hiding the unused dirs is fine, too. (Using more than one backends
simultaneously really isn't a requirement now (that not even one works ;) ).)
